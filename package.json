{
  "name": "bybit-perp-screener",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Screener en tiempo real de todos los perpetuos de Bybit con volume delta por timeframe",
  "engines": {
    "node": ">=22.4"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "mock": "MOCK=1 node server/index.js",
    "test": "node --test test/*.test.js"
  }
}
